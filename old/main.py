import pygame

pygame.init()

HEIGHT, WIDTH = 1000, 700
SCREEN = pygame.display.set_mode((HEIGHT, WIDTH), pygame.RESIZABLE)
pygame.display.set_caption('we\'re making the game')

def gameFunc():
    pass
